# Phase 12.1 Owner Demo Report

Date: 2026-07-29

## Result

The private owner-demo profile is live on the existing `OWNER_ONLY` Black Hole
server. It loads CRM data only for the owner of the server-side personal API
key. No Bitrix24 placement was created or changed.

- Production URL: `https://app-b19d2b35af4a.vibecode.bitrix24.tech`
- Server: `c318fdf4-8ac1-485d-8bfc-82eb87d2b872`
- Deployed commit: `528d244`
- Source snapshot: `v9` (unchanged; Phase 13 used an allowlisted archive)
- Server policy: `OWNER_ONLY`
- Session mode: `owner-api-key`

## Runtime Profile

Only server-side deployment environment contains values for these names:

```env
NODE_ENV=production
VIBECODE_API_BASE_URL=https://vibecode.bitrix24.tech/v1
VIBECODE_API_KEY=<personal owner API key>
SESSION_CONTEXT_MODE=owner-api-key
OWNER_DEMO_PORTAL=<single allowed Bitrix24 portal domain>
```

The browser supplies no portal, Bitrix24 user, VibeCode key, or session token.
The backend validates that the server-side key belongs to the configured portal
before using read-only CRM endpoints.

## Production Smoke

The smoke used a short-lived `api-bearer` token and reported no CRM values.

| Check | Result |
| --- | --- |
| `/health` | `200` |
| `/ready` | `200`, version `528d244`, mode `owner-api-key` |
| Frontend root | `200`, contains `id="app"` |
| `/_gw/icon` | `200`, `image/png` |
| `/api/bootstrap` | `200` |
| `/api/dashboard` | `200` |
| CORS allowed origin | returned for the configured portal |
| CSP and `frame-ancestors` | present |

The normalized bootstrap response contained four accessible categories. The
dashboard returned `200` for categories 2, 4, 6, and 8. Each response used the
selected category's stage references; category 2 included the three newly
created recent records, and the trend returned 30 points. Only statuses,
counts, identifiers needed for smoke, and DTO shape were inspected; no CRM
payload was saved in the repository.

## Dashboard Data Accuracy Update

- Selected pipeline stages are loaded after filter validation, so every
  category receives its own `DEAL_STAGE_<id>` references.
- The backend makes one bounded, selected-category deal snapshot request and
  derives all KPI, funnel, money, trend, and recent-deal values locally. This
  avoids the upstream final-day date-filter defect and prevents process deals
  with planned close dates from being counted as successful.
- Trend periods are zero-filled and the frontend renders created and successful
  values as grouped bars. Decimal and hexadecimal numeric currency entities,
  including the ruble symbol, are decoded before rendering.
- Changed files: `backend/src/services/referenceDataService.ts`,
  `backend/src/http/routes/dashboard.ts`,
  `backend/src/services/dealsQueryService.ts`,
  `backend/src/services/aggregationService.ts`, their backend tests,
  `dashboard-app/src/components/dashboard/TrendChart.vue`,
  `dashboard-app/src/components/dashboard/dashboardFormatters.ts`, related
  frontend tests, `docs/12.1-owner-demo-report.md`, and `docs/06-plan.md`.
- Checks: 107 backend tests, backend typecheck and lint, production build,
  artifact check, 42 frontend tests, frontend typecheck and lint, and secret
  scan passed. The 71-entry deployment archive had zero forbidden entries.

## Phase 13 Live Diagnostic

A short-lived `api-bearer` token was used for one read-only `last30` request
per selected category and revoked in the same command. No CRM records, payloads,
or credentials were retained.

| Category | API status | Stage prefix | Winner count | Money rows | Trend points | Non-zero trend points | Warnings |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2 | `200` | `DEAL_STAGE_2` | 2 | 1 | 30 | 2 | None |
| 4 | `200` | `DEAL_STAGE_4` | 1 | 1 | 30 | 1 | None |
| 6 | `200` | `DEAL_STAGE_6` | 1 | 1 | 30 | 1 | None |
| 8 | `200` | `DEAL_STAGE_8` | 1 | 1 | 30 | 1 | None |

Category 2 matches the accepted business fact: two successful deals total
`500000233` RUB. This confirms that the outstanding stale-trend defect is at
the chart-rendering boundary, not the production CRM aggregation endpoint.

## Phase 13 Release

- Done: production now uses commit `528d244`. `TrendChart` has a deterministic
  render key, so Unovis replaces its container when a selected category returns
  a different trend. The chart contract is covered by a component test. Backend
  aggregation was intentionally not changed: live diagnostic and regression
  tests confirm its existing winner totals and selected-category references.
- Changed files: `dashboard-app/src/components/dashboard/TrendChart.vue`,
  `dashboard-app/src/components/dashboard/__tests__/TrendChart.test.ts`,
  `dashboard-app/vitest.config.ts`,
  `backend/tests/services/aggregationService.test.ts`,
  `backend/tests/http/dashboard.test.ts`, `docs/12.1-owner-demo-report.md`,
  and `docs/06-plan.md`.
- Checks: 109 backend tests, backend typecheck and lint, production build,
  artifact check, 43 frontend tests, frontend typecheck and lint, and secret
  scan passed. The release archive had 71 entries and zero forbidden entries.
  Production `/health`, `/ready`, `/api/bootstrap`, and each selected dashboard
  category returned `200`; the temporary smoke token was revoked. A 20-line
  app-log scan found zero request failures and zero key, bearer, session, or
  email patterns.

## Security Checks

- The final archive had 71 entries and zero forbidden entries.
- Backend production build, artifact check, and secret scan passed.
- Temporary Phase 12.1 access tokens were revoked. The later token list had
  zero tokens named for Phase 12.1; one unrelated existing token remains.
- Runtime log scan found zero app/API-key, bearer, session-token, and email
  patterns.

## Manual Screenshot Step

Sign in to VibeCode as the server owner, open the production URL, and capture
screenshots after confirming the dashboard and filters render. Do not share
the URL or use this profile for a client portal.

## Rollback To Client Integration

Redeploy with the future client profile:

```env
VIBECODE_API_BASE_URL=https://vibecode.bitrix24.tech/v1
VIBECODE_APP_KEY=<OAuth application key>
SESSION_CONTEXT_MODE=gateway-headers
```

Remove `VIBECODE_API_KEY` and `OWNER_DEMO_PORTAL` from that deploy profile.
Then confirm an unauthenticated dashboard API request returns `401
AUTH_REQUIRED` before enabling any Bitrix24 placement.

## Remaining Risks

- This profile is intentionally single-owner and must never be made public or
  used as client delivery.
- It does not complete Phase 12 placement, per-user CRM rights, or restricted
  user acceptance.
- The personal API key was previously shared in chat and must be rotated
  before long-term use.
- The frontend still emits the existing large-chunk build warning.
- A selected-category snapshot is capped at 500 deals. At that limit the API
  returns `PARTIAL_AGGREGATION` with `truncatedBlocks: ['snapshot']`; values
  should be treated as partial until pagination is introduced.
- The final visual confirmation of switching between two pipelines remains a
  manual owner screenshot step; automated coverage verifies the component's
  data replacement contract.

## Phase 14 Dashboard UI Release

Date: 2026-07-30

- Production source snapshot `v11` is live on the existing `OWNER_ONLY`
  server. The server-side owner API key and all other existing environment
  values were preserved during the clean deploy.
- The dashboard defaults to light mode, has no internal sidebar, and includes
  an icon-only theme control. Pipeline selection is global at the top, while
  period and currency controls sit with the trend and update every dashboard
  block together.
- Stage data is now rendered as compact trapezoid funnel layers. The analytics
  row assigns one third of the desktop width to the funnel and two thirds to
  the trend, with a one-column mobile layout at 860px and below.
- The trend is a one-series smooth area/line chart with a Created/Won selector.
  It measures the available card width, clips its viewport, and thins labels.
  API bucketing is daily for `last7`, weekly for `last30`, `last90`,
  `currentMonth`, and `previousMonth`; production smoke returned respectively
  `7`, `5`, `14`, `5`, and `5` points for category `2`.

### Verification

- Local checks: 110 backend tests, backend typecheck/lint/production build/
  artifact check; 51 frontend tests, frontend typecheck/lint/secret scan.
- Deployment archive: 71 allowlisted entries, zero forbidden entries.
- Production: deploy healthcheck passed; `/health`, frontend root, bootstrap,
  and dashboards for categories `2`, `4`, `6`, and `8` returned successfully.
  All four category `last30` requests used weekly buckets with five points.
- Security: a five-minute `api-bearer` smoke token was revoked in `finally`;
  follow-up inspection found zero active Phase 14 smoke tokens. A 41-line
  app-log scan found zero request-failure, credential, or PII patterns.

### Remaining Risks

- This remains a single-owner screenshot profile, not a client placement or
  per-user CRM-rights implementation.
- The personal key previously shared in chat should be rotated.
- A selected-category snapshot is limited to 500 deals and is marked partial
  at that limit; pagination remains future work.
- The production build retains the existing large-chunk warning.
- The server-side `DEPLOYMENT_VERSION` was preserved with the secret env and
  still reports the earlier value; source snapshot `v11` is the deploy
  identifier for this release.
- Final browser screenshot acceptance is still an owner action because this
  workspace has no local browser automation.

## Phase 14.1 Dashboard Visual Polish Release

Date: 2026-07-30

- The trend is now one colored smooth line on a transparent card surface:
  blue for Created and green for Won. `VisArea` was removed, so neither theme
  can render the previous black or blue filled area. Neutral thin grid and
  axis styles remain visible in light and dark modes.
- The desktop chart canvas is 380px tall. Width constraints now exist at the
  shell, analytics grid, card, and chart viewport levels; the responsive
  heights at 860px and 520px preserve the existing one-column layout without
  horizontal overflow.
- Funnel layers use a centered name/count row and a centered money row beneath
  it. Their reduced padding and spacing make the stage card compact while
  retaining zero-count layers.

### Verification

- Local: focused visual-regression tests `10/10`; frontend tests `52/52`,
  typecheck, lint, production build, secret scan, and license scan; backend
  tests `110/110`, typecheck, lint, build, artifact check, secret scan, and
  license scan.
- Deployment: the allowlisted archive contained 71 entries and zero forbidden
  files. Its first clean deployment returned `503` from `/ready` because
  `cleanDeploy` removed `/opt/app/.env`, the systemd environment file. The
  same archive was redeployed with the required owner-demo environment passed
  directly to the Infra deploy API; secret values were not stored in the
  repository or release artifact.
- Production: `/health`, `/ready`, frontend root, and bootstrap returned
  `200`. All 20 requests for categories `2`, `4`, `6`, and `8` across `last7`,
  `last30`, `last90`, `currentMonth`, and `previousMonth` returned non-empty
  trend and funnel data. The temporary `api-bearer` token was revoked in
  `finally`; a 50-line log scan found zero request-failure, credential, or PII
  patterns.

### Remaining Risks

- The application remains an `OWNER_ONLY` screenshot demo, not a client
  placement or per-user CRM-rights implementation.
- The personal key previously shared in chat must be rotated.
- Future `cleanDeploy` releases must provide the complete owner-demo runtime
  environment in the deployment request, otherwise `/ready` and CRM routes
  will fail configuration validation.
- The existing 500-deal partial-aggregation behavior, frontend chunk warning,
  and manual browser acceptance check for both themes remain.

## Trend SVG Fill Hotfix

Date: 2026-07-30

- The black trend area was caused by `VisLine` paths relying on a runtime
  Unovis `fill: none` style that was absent in the deployed page. The dashboard
  now owns a scoped `.dashboard-trend-chart svg path` fallback
  with `fill: none`, so the default SVG black fill cannot return.
- Created remains blue and Won remains green. The fix does not change CRM
  requests, aggregations, filters, axes, grid, tooltips, or funnel rendering.

### Verification

- Local: focused quality test `6/6`; frontend full tests `52/52`, typecheck,
  lint, production build, secret scan, and license scan; backend build,
  artifact check, and secret scan.
- Deployment: a 71-entry allowlisted archive with zero forbidden files was
  deployed with the required owner-demo runtime environment supplied only to
  the Infra deploy request.
- Production: `/health`, `/ready`, root, and bootstrap returned `200`; four
  pipeline dashboard smoke requests returned non-empty trend and stage data.
  The temporary `api-bearer` token was revoked in `finally`; the 50-line log
  scan found zero request-failure, credential, or PII patterns.
- Production correction: after changing the selector to the actual SVG
  hierarchy, the delivered CSS asset contained the new rule; `/health`,
  `/ready`, root, bootstrap, and dashboard requests for categories `2`, `4`,
  `6`, and `8` returned `200`. Owner screenshots for `last7`, `last30`,
  `last90`, and `currentMonth` confirm the black fill is gone.

### Remaining Risk

- The existing owner-only demo profile, key-rotation requirement, 500-deal
  partial-aggregation behavior, and frontend large-chunk warning remain.

## Phase 15 Open Deals Amount Deployment

Date: 2026-07-30

- Production was updated to commit `49a13ea`; VibeCode saved source snapshot
  `v15`. The allowlisted archive contained only backend runtime files and the
  built frontend. `cleanDeploy` used `preserveEnv`, so the owner-demo runtime
  profile remained server-side and no secret value was added to the archive.
- Deployment healthcheck, tunnel routing, `/health`, `/ready`, frontend root,
  and `/api/bootstrap` returned `200`. Dashboard smoke requests for categories
  `2`, `4`, `6`, and `8` also returned `200`; every response contained
  `kpi.openNow.amountsByCurrency` alongside the open-deal count.
- The smoke used one temporary `api-bearer` token, which was revoked in a
  `finally` block. Visual confirmation of the compact caption and tooltip
  remains an owner browser action.
